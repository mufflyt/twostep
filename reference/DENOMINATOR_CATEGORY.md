# Total-female access-table denominator category label

The \`category\` value marking the all-female-population row in the
access tables (\`filter(category == DENOMINATOR_CATEGORY)\`). Live from
\`mufflyaccess::DENOMINATOR_CATEGORY\`.

## Usage

``` r
DENOMINATOR_CATEGORY
```

## Format

Character scalar.
