# Default access thresholds (in per-100k units) for cell-level population shares.

Default access thresholds (in per-100k units) for cell-level population
shares.

## Usage

``` r
E2SFCA_DEFAULT_THRESHOLDS
```
