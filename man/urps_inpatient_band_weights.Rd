% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/urps_inpatient_sigma.R
\name{urps_inpatient_band_weights}
\alias{urps_inpatient_band_weights}
\title{E2SFCA band weights for urogynaecologic inpatient surgery}
\usage{
urps_inpatient_band_weights(
  bands = c(30L, 60L, 120L, 180L),
  sigma_minutes = URPS_INPATIENT_SIGMA_MINUTES
)
}
\arguments{
\item{bands}{Band edges in minutes, as for \code{\link{gaussian_band_weights}}.}

\item{sigma_minutes}{Calibrated bandwidth in minutes; defaults to the
road-network-calibrated \code{\link{URPS_INPATIENT_SIGMA_MINUTES}} (41).}
}
\value{
Named numeric vector of normalised zonal weights.
}
\description{
Wraps \code{\link{gaussian_band_weights}} with the CHIA-calibrated bandwidth. Returns a
monotone vector that passes \code{\link{e2sfca_band_weights}}, unlike the raw empirical
kernel, which is non-monotone (women bypass nearer hospitals for
higher-volume ones) and would produce a negative incremental weight.
}
\examples{
urps_inpatient_band_weights()   # 1.000 0.448 0.018 0.000  (sigma = 41 min)
gaussian_band_weights()         # 1.000 0.687 0.153 0.013  (sigma = 60 min)
}
