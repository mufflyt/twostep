% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two_step_floating_catchment.R
\name{gaussian_decay_weights}
\alias{gaussian_decay_weights}
\title{RAW continuous Gaussian decay kernel G(d) = exp(-d^2 / (2 sigma^2)).}
\usage{
gaussian_decay_weights(minutes, sigma = 60)
}
\arguments{
\item{minutes}{Numeric travel times in minutes (>= 0).}

\item{sigma}{Gaussian bandwidth in minutes, strictly positive.}
}
\value{
Named numeric vector G(minutes) in (0, 1], names = the minutes.
}
\description{
The un-normalized kernel: G(0) = 1 and G decreases monotonically. This is the
continuous decay function; it is NOT the zonal band weights (which normalize to
the inner band, see [gaussian_band_weights]). A genuinely continuous E2SFCA
would apply this to a per provider-to-cell travel time. Do NOT normalize this to
the 30-min value: distances < 30 min would then exceed 1.
}
\examples{
gaussian_decay_weights(c(0, 30, 60, 120), sigma = 60)
# G(0) = 1, then 0.882, 0.607, 0.135 : the RAW kernel, not normalized to band 1
}
\references{
Gaussian decay as a 2SFCA impedance function: McGrail & Humphreys
  (2009) doi:10.1016/j.apgeog.2008.12.003 and McGrail (2012)
  doi:10.1186/1476-072X-11-50 [source 5]; the Gaussian form also appears in
  Luo & Qi (2009) [source 2] as one of the tested decay weights.
}
\seealso{
[gaussian_band_weights] (the normalized zonal wrapper actually used)

Other E2SFCA distance-decay weights:
\code{\link[=e2sfca_band_weights]{e2sfca_band_weights()}},
\code{\link[=e2sfca_incremental_weights]{e2sfca_incremental_weights()}},
\code{\link[=gaussian_band_weights]{gaussian_band_weights()}}
}
\concept{E2SFCA distance-decay weights}
